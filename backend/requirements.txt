# Web框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 数据验证
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 配置管理
pyyaml>=6.0.1
python-dotenv>=1.0.0

# 日志
loguru>=0.7.2

# 数据库
sqlalchemy>=2.0.23
aiosqlite>=0.19.0

# 通达信
mootdx>=0.9.11

# 技术指标
ta-lib>=0.4.28

# HTTP客户端
httpx>=0.27.0
aiohttp>=3.9.1

# Claude AI SDK
anthropic>=0.40.0

# OpenAI SDK (用于调用GLM的OpenAI兼容API)
openai>=1.0.0

# Claude Agent SDK
claude-agent-sdk>=0.1.0

# 智谱GLM SDK - 已移除，统一使用Claude Agent SDK
# zhipuai>=2.1.0  # 不再需要，通过Claude Agent SDK调用GLM

# 智谱联网搜索SDK
zai-sdk>=0.2.2

# 浏览器自动化（可选，用于增强搜索）
playwright>=1.40.0

# Agent框架
anthropic>=0.40.0
sentence-transformers>=2.2.0
numpy>=1.26.2

# 工具
python-dateutil>=2.8.2
pandas>=2.1.4
numpy>=1.26.2

# 文件解析
pdfplumber>=0.10.0
python-docx>=1.1.0

# PDF 生成（后端备用方案，前端优先使用 Electron Chromium 渲染）
fpdf2>=2.8.0
markdown>=3.5.0

# 飞书 SDK
lark-oapi>=1.6.0

# 定时任务调度
apscheduler>=3.10.0

# 交易日历
baostock>=0.9.2

# 开发依赖
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
pytest-mock>=3.12.0
black>=23.11.0
ruff>=0.1.6
mypy>=0.1.6
types-PyYAML>=6.0.12
