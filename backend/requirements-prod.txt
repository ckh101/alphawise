# 生产运行时依赖清单（Win/Mac 共用）
#
# 从 requirements.txt 派生，剔除代码无 import 的包：
#   - ta-lib (代码无 import，且需 C 库)
#   - sentence-transformers (代码无 import，会拖入 torch)
#   - markdown (代码无 import，渲染在前端 marked.js)
#   - python-dotenv (代码无 import，配置走 yaml)
#   - python-dateutil (代码无 import)
#   - pydantic-settings (代码无 import)
#   - black / mypy / ruff (开发期 lint/格式工具)
#   - pytest / pytest-asyncio / pytest-cov / pytest-mock (测试)
#   - types-PyYAML (类型存根，仅 mypy 用)
#
# dev 环境继续使用 requirements.txt（保持不变）。

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 数据验证
pydantic>=2.5.0

# 配置管理
pyyaml>=6.0.1

# 日志
loguru>=0.7.2

# 数据库
sqlalchemy>=2.0.23
aiosqlite>=0.19.0

# 通达信
mootdx>=0.9.11

# HTTP 客户端（mootdx / zai-sdk 间接依赖）
httpx>=0.27.0

# Claude AI（claude_client.py 直接 import；anthropic 也被 claude-agent-sdk / openai 间接依赖）
anthropic>=0.40.0

# OpenAI SDK（调用 GLM 的 OpenAI 兼容 API）
openai>=1.0.0

# Claude Agent SDK（Agent 编排核心，含 bundled claude.exe）
claude-agent-sdk>=0.1.0

# 智谱联网搜索 SDK
zai-sdk>=0.2.2

# 浏览器自动化（增强搜索）
playwright>=1.40.0

# 定时任务调度
apscheduler>=3.10.0

# 交易日历
baostock>=0.9.2

# 数据处理
pandas>=2.1.4
numpy>=1.26.2

# 文件解析
pdfplumber>=0.10.0
python-docx>=1.1.0

# PDF 生成（后端备用方案）
fpdf2>=2.8.0

# 飞书 SDK
lark-oapi>=1.6.0
